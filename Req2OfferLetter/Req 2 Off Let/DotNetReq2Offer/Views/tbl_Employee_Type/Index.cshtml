@model IEnumerable<DotNetReq2Offer.Models.tbl_Employee_Type>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Employee_Type)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Employee_Type)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Type_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Type_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Type_Id })
        </td>
    </tr>
}

</table>
